export async function GET()  { return Response.json({ ok: true, message: "Billing disabled on preview." }); }
export async function POST() { return Response.json({ ok: true, message: "Billing disabled on preview." }); }
