# ---- API Server ----
API_HOST=0.0.0.0
API_PORT=8000

# ---- Redis ----
# If running via docker, 'redis' is the hostname from docker-compose.
# For local dev without Docker, use redis://localhost:6379/0
REDIS_URL=redis://redis:6379/0

# ---- CORS ----
# Comma-separated list of allowed origins (frontend URLs)
CORS_ORIGINS=http://localhost:3000

# ---- Auth ----
# Must match apps/web/.env AUTH_SECRET exactly
AUTH_SECRET=changeme_auth_secret
# Optionally also include NEXTAUTH_SECRET for compatibility
NEXTAUTH_SECRET=changeme_nextauth_secret
