# Auth / JWT (must match web)
AUTH_SECRET=__REPLACE_ME_STRONG_RANDOM__
AUTH_JWT_ALG=HS256
AUTH_JWT_ISS=winclm
AUTH_JWT_AUD=api

# CORS
CORS_ORIGINS=http://localhost:3000

# Server
API_HOST=0.0.0.0
API_PORT=8000

# Redis
REDIS_URL=redis://redis:6379

# (Only add if API handles Stripe webhooks too)
# STRIPE_WEBHOOK_SECRET=whsec_xxx
