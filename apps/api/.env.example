API_HOST=0.0.0.0
API_PORT=8000

# If running via docker, 'redis' is the hostname from compose; otherwise use localhost.
REDIS_URL=redis://redis:6379/0
# For local (no docker): REDIS_URL=redis://localhost:6379/0

# Comma-separated list of allowed origins for CORS
CORS_ORIGINS=http://localhost:3000

# 🔐 Must match apps/web/.env AUTH_SECRET exactly
AUTH_SECRET=changeme

