API_HOST=0.0.0.0
API_PORT=8000
# If running via docker, 'redis' is the hostname from compose; otherwise use localhost.
REDIS_URL=redis://redis:6379/0
# Comma-separated list
CORS_ORIGINS=http://localhost:3000
